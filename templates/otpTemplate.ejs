<div class="otp-container">
    <div class="otp-hero">
        <div class="otp-icon">🔑</div>
        <h1 class="otp-title">Your OTP Code</h1>
        <p class="otp-subtitle">Secure access to your account</p>
    </div>
    
    <div class="otp-content">
        <div class="otp-message">
            <p>Hello! 👋</p>
            <p>Here's your One-Time Password (OTP) to access your account. This code is valid for a limited time and should be kept confidential.</p>
        </div>
        
        <div class="otp-display-section">
            <div class="otp-label">Enter this code:</div>
            <div class="otp-code">
                <span class="otp-character"><%= otp.charAt(0) %></span>
                <span class="otp-character"><%= otp.charAt(1) %></span>
                <span class="otp-character"><%= otp.charAt(2) %></span>
                <span class="otp-character"><%= otp.charAt(3) %></span>
                <span class="otp-character"><%= otp.charAt(4) %></span>
                <span class="otp-character"><%= otp.charAt(5) %></span>
            </div>
            <p class="otp-instruction">Copy and paste this code into the verification field</p>
        </div>
        
        <div class="otp-details">
            <div class="detail-item">
                <div class="detail-icon">⏱️</div>
                <div class="detail-content">
                    <h4>Expires Soon</h4>
                    <p>This code will expire in 10 minutes</p>
                </div>
            </div>
            <div class="detail-item">
                <div class="detail-icon">🚫</div>
                <div class="detail-content">
                    <h4>Don't Share</h4>
                    <p>Never share this code with anyone</p>
                </div>
            </div>
            <div class="detail-item">
                <div class="detail-icon">📱</div>
                <div class="detail-content">
                    <h4>One-Time Use</h4>
                    <p>This code can only be used once</p>
                </div>
            </div>
        </div>
        
        <div class="otp-footer">
            <p>If you didn't request this code, please ignore this email or contact our support team immediately.</p>
            <div class="support-links">
                <a href="mailto:info@tourbirth.com" class="support-link">📧 Contact Support</a>
                <span class="separator">|</span>
                <a href="#" class="support-link">🔄 Request New Code</a>
            </div>
        </div>
    </div>
</div>